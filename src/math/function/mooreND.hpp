#ifndef math_function_mooreND
#define math_function_mooreND

 // ::math::function::mooreND

namespace math
 {
  namespace function
   {

    template < typename scalar_name, ::math::type::size_type dimension_number, typename size_name = unsigned >
     void mooreND( std::array<scalar_name,dimension_number> & coord, scalar_name value, size_name iteration=16 )
      {
       // TODO
      }
   }
 }

#endif
